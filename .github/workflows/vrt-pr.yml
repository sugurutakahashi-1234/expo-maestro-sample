name: VRT PR Comparison

# PRが作成・更新されたときにVRT比較を実行
on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: write       # peaceiris/actions-gh-pages が gh-pages にpush
  pull-requests: write  # PRコメント投稿
  statuses: write       # コミットステータス設定
  pages: write          # GitHub Pages への書き込み
  id-token: write       # OIDC トークン検証用

jobs:
  # ========================================
  # Maestro VRT
  # ========================================
  maestro-vrt:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v5
        with:
          fetch-depth: 0  # 全履歴を取得

      - name: Check VRT conditions
        id: check
        run: |
          echo "🔍 Checking Maestro VRT execution conditions..."

          # コード系ファイルの変更チェック（.ts, .tsx, .js, .jsx, .json, .yaml）
          CODE_CHANGED=$(git diff --name-only ${{ github.event.pull_request.base.sha }} HEAD | grep -E '\.(ts|tsx|js|jsx|json|yaml)$' || echo "")

          # スクリーンショットの変更チェック
          SCREENSHOTS_CHANGED=$(git diff --name-only ${{ github.event.pull_request.base.sha }} HEAD | grep -E '^apps/cool-app/\.maestro/screenshots/' || echo "")

          # スキップ判定（どちらか一方があれば実行）
          if [ -z "$CODE_CHANGED" ] && [ -z "$SCREENSHOTS_CHANGED" ]; then
            echo "should_run=false" >> $GITHUB_OUTPUT
            echo "skip_reason=コード変更もスクリーンショット変更も検出されませんでした" >> $GITHUB_OUTPUT
            echo "⏭️ Maestro VRT Skipped: No code or screenshot changes detected"
          else
            echo "should_run=true" >> $GITHUB_OUTPUT
            echo "✅ Maestro VRT conditions met - proceeding with comparison"
            if [ -n "$CODE_CHANGED" ]; then
              echo "  - Code changes detected"
            fi
            if [ -n "$SCREENSHOTS_CHANGED" ]; then
              echo "  - Screenshot changes detected"
            fi
          fi

      - name: Comment PR - Maestro VRT Skipped
        if: steps.check.outputs.should_run == 'false'
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: maestro-vrt-skipped
          message: |
            ## ⏭️ Maestro VRT Skipped

            **理由**: ${{ steps.check.outputs.skip_reason }}

            ---

            💡 **Maestro VRTを実行するには以下のいずれかの変更が必要です:**
            - コード変更（`.ts`, `.tsx`, `.js`, `.jsx`, `.json`, `.yaml`）
            - スクリーンショット変更（`apps/cool-app/.maestro/screenshots/`）

            スクリーンショットを生成するには:
            ```bash
            cd apps/cool-app
            bun run maestro:all # Maestroテスト実行（スクリーンショット取得）
            ```

      - name: Setup Bun
        if: steps.check.outputs.should_run == 'true'
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        if: steps.check.outputs.should_run == 'true'
        run: bun install --frozen-lockfile

      - name: Authenticate to Google Cloud
        if: steps.check.outputs.should_run == 'true'
        uses: google-github-actions/auth@v3
        with:
          credentials_json: '${{ secrets.VRT_GCS_CREDENTIALS_JSON }}'

      - name: Workaround for detached HEAD
        if: steps.check.outputs.should_run == 'true'
        run: |
          git checkout ${{ github.head_ref }}

      - name: Compare with baseline
        if: steps.check.outputs.should_run == 'true'
        id: comparison-keys
        working-directory: apps/cool-app
        run: |
          # ベース側（EXPECTED）の変数を構築
          BASE_HASH=$(git rev-parse --short=7 ${{ github.event.pull_request.base.sha }})
          BASE_VERSION=$(git show ${{ github.event.pull_request.base.sha }}:apps/cool-app/app.json | jq -r '.expo.version')
          BASE_BRANCH="${{ github.event.pull_request.base.ref }}"
          BASE_BRANCH_SAFE=$(echo "$BASE_BRANCH" | sed 's/[^a-zA-Z0-9._-]/_/g')
          EXPECTED_KEY="$BASE_BRANCH_SAFE/$BASE_VERSION/$BASE_HASH/maestro"

          # PR側（ACTUAL）の変数を構築
          PR_HASH=$(git rev-parse --short=7 HEAD)
          PR_VERSION=$(npx expo config --json | jq -r '.version')
          PR_BRANCH="${{ github.head_ref }}"
          PR_BRANCH_SAFE=$(echo "$PR_BRANCH" | sed 's/[^a-zA-Z0-9._-]/_/g')
          ACTUAL_KEY="$PR_BRANCH_SAFE/$PR_VERSION/$PR_HASH/maestro"

          echo "🔍 Maestro VRT Comparison Keys"
          echo "EXPECTED_KEY: $EXPECTED_KEY"
          echo "ACTUAL_KEY: $ACTUAL_KEY"
          echo ""

          # Output for PR comment
          echo "base-branch=$BASE_BRANCH" >> $GITHUB_OUTPUT
          echo "base-branch-safe=$BASE_BRANCH_SAFE" >> $GITHUB_OUTPUT
          echo "base-version=$BASE_VERSION" >> $GITHUB_OUTPUT
          echo "base-hash=$BASE_HASH" >> $GITHUB_OUTPUT
          echo "pr-branch=$PR_BRANCH" >> $GITHUB_OUTPUT
          echo "pr-branch-safe=$PR_BRANCH_SAFE" >> $GITHUB_OUTPUT
          echo "pr-version=$PR_VERSION" >> $GITHUB_OUTPUT
          echo "pr-hash=$PR_HASH" >> $GITHUB_OUTPUT
          echo "expected-key=$EXPECTED_KEY" >> $GITHUB_OUTPUT
          echo "actual-key=$ACTUAL_KEY" >> $GITHUB_OUTPUT

          # reg-suit で比較
          rm -rf .reg
          ACTUAL_DIR=.maestro/screenshots \
          EXPECTED_KEY=$EXPECTED_KEY \
          ACTUAL_KEY=$ACTUAL_KEY \
          bunx reg-suit run

          # 比較結果の詳細を出力
          echo ""
          echo "📁 .reg directory structure:"
          ls -lah .reg/
          echo ""
          echo "📄 VRT comparison result (out.json):"
          jq '.' .reg/out.json

          # VRT結果を解析
          CHANGED=$(jq '.diffItems | length' .reg/out.json)
          PASSED=$(jq '.passedItems | length' .reg/out.json)
          NEW=$(jq '.newItems | length' .reg/out.json)
          DELETED=$(jq '.deletedItems | length' .reg/out.json)

          # テーブル行を動的に生成（0以外のみ）
          VRT_TABLE="| Status | Count |
          |--------|-------|"
          [ "$CHANGED" -gt 0 ] && VRT_TABLE="$VRT_TABLE
          | 🔴 Changed | $CHANGED |"
          [ "$PASSED" -gt 0 ] && VRT_TABLE="$VRT_TABLE
          | 🟢 Passed | $PASSED |"
          [ "$NEW" -gt 0 ] && VRT_TABLE="$VRT_TABLE
          | 🆕 New | $NEW |"
          [ "$DELETED" -gt 0 ] && VRT_TABLE="$VRT_TABLE
          | 🗑️ Deleted | $DELETED |"

          # multiline output
          echo "vrt-table<<EOF" >> $GITHUB_OUTPUT
          echo "$VRT_TABLE" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Set GitHub Pages path
        if: steps.check.outputs.should_run == 'true'
        id: pages-path
        run: |
          PR_HASH=$(git rev-parse --short=7 HEAD)
          PR_VERSION=$(cd apps/cool-app && npx expo config --json | jq -r '.version')
          PR_BRANCH="${{ github.head_ref }}"
          PR_BRANCH_SAFE=$(echo "$PR_BRANCH" | sed 's/[^a-zA-Z0-9._-]/_/g')
          PAGES_PATH="$PR_BRANCH_SAFE/$PR_VERSION/$PR_HASH/maestro"
          echo "path=$PAGES_PATH" >> $GITHUB_OUTPUT
          echo "📍 GitHub Pages path: $PAGES_PATH"

      - name: Deploy to GitHub Pages
        if: steps.check.outputs.should_run == 'true' && always()
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: apps/cool-app/.reg
          destination_dir: ${{ steps.pages-path.outputs.path }}
          keep_files: true

      - name: Upload VRT report
        if: steps.check.outputs.should_run == 'true' && always()
        uses: actions/upload-artifact@v5
        with:
          name: maestro-vrt-report
          path: apps/cool-app/.reg/
          retention-days: 30
          include-hidden-files: true

      - name: Comment PR with VRT access methods
        if: steps.check.outputs.should_run == 'true' && always()
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: maestro-vrt-report
          message: |
            ## 🎼 Maestro VRT Report

            ${{ steps.comparison-keys.outputs.vrt-table }}

            🔗 **View Report**: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/${{ steps.pages-path.outputs.path }}/

            <details>
            <summary>📋 詳細情報</summary>

            ### ⏱️ 保存期間
            このレポートは **30日間保存** されます（その後自動削除）。

            ### 💻 ローカルでレポートを表示
            ```bash
            cd apps/cool-app
            rm -rf .reg
            mkdir -p .reg
            gsutil -m cp -r "gs://vrt-sample/${{ steps.comparison-keys.outputs.actual-key }}" .reg
            open .reg/maestro/index.html
            ```

            ### 📊 比較情報
            - **Base**: `${{ steps.comparison-keys.outputs.expected-key }}`
            - **PR**: `${{ steps.comparison-keys.outputs.actual-key }}`

            ---
            *Powered by reg-suit + GitHub Pages*

            </details>

      - name: Summary
        if: steps.check.outputs.should_run == 'true' && always()
        run: |
          BASE_HASH=$(git rev-parse --short=7 ${{ github.event.pull_request.base.sha }})
          PR_HASH=$(git rev-parse --short=7 HEAD)
          echo "## Maestro VRT Comparison" >> $GITHUB_STEP_SUMMARY
          echo "- Base: $BASE_HASH" >> $GITHUB_STEP_SUMMARY
          echo "- PR: $PR_HASH" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ Maestro VRT比較が完了しました。詳細はアーティファクトのレポートを確認してください。" >> $GITHUB_STEP_SUMMARY

  # ========================================
  # Playwright VRT
  # ========================================
  # 注: Playwright公式Dockerイメージ（mcr.microsoft.com/playwright:v1.56.1-noble）を
  # 使用してブラウザインストール時間の短縮を試みたが、コンテナ内でunzipが不在のため
  # apt-get installが必要となり、結果的に従来のアプローチと同等の時間がかかった。
  # 参考: https://stackoverflow.com/questions/76095224
  playwright-vrt:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Check VRT conditions
        id: check
        run: |
          echo "🔍 Checking Playwright VRT execution conditions..."

          # コード系ファイルの変更チェック
          CODE_CHANGED=$(git diff --name-only ${{ github.event.pull_request.base.sha }} HEAD | grep -E '\.(ts|tsx|js|jsx|json|yaml)$' || echo "")

          if [ -z "$CODE_CHANGED" ]; then
            echo "should_run=false" >> $GITHUB_OUTPUT
            echo "skip_reason=コード変更が検出されませんでした" >> $GITHUB_OUTPUT
            echo "⏭️ Playwright VRT Skipped: No code changes detected"
          else
            echo "should_run=true" >> $GITHUB_OUTPUT
            echo "✅ Playwright VRT conditions met - proceeding with comparison"
          fi

      - name: Comment PR - Playwright VRT Skipped
        if: steps.check.outputs.should_run == 'false'
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: playwright-vrt-skipped
          message: |
            ## ⏭️ Playwright VRT Skipped

            **理由**: ${{ steps.check.outputs.skip_reason }}

            ---

            💡 **Playwright VRTを実行するにはコード変更が必要です:**
            - コード変更（`.ts`, `.tsx`, `.js`, `.jsx`, `.json`, `.yaml`）

      - name: Setup Bun
        if: steps.check.outputs.should_run == 'true'
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        if: steps.check.outputs.should_run == 'true'
        run: bun install --frozen-lockfile

      # --with-deps: WebKitはubuntu-latestに含まれていないシステムライブラリ
      # （libgtk-4, libgraphene等）を必要とするため、依存関係も一緒にインストール
      - name: Install Playwright browsers
        if: steps.check.outputs.should_run == 'true'
        working-directory: apps/cool-app
        run: bunx playwright install --with-deps chromium webkit

      - name: Run Playwright tests
        if: steps.check.outputs.should_run == 'true'
        working-directory: apps/cool-app
        run: bun run playwright:headless
        env:
          CI: true

      - name: Set GitHub Pages path
        if: steps.check.outputs.should_run == 'true'
        id: pages-path
        run: |
          PR_HASH=$(git rev-parse --short=7 HEAD)
          PR_VERSION=$(cd apps/cool-app && npx expo config --json | jq -r '.version')
          PR_BRANCH="${{ github.head_ref }}"
          PR_BRANCH_SAFE=$(echo "$PR_BRANCH" | sed 's/[^a-zA-Z0-9._-]/_/g')
          PAGES_PATH="$PR_BRANCH_SAFE/$PR_VERSION/$PR_HASH/playwright"
          echo "path=$PAGES_PATH" >> $GITHUB_OUTPUT
          echo "📍 GitHub Pages path: $PAGES_PATH"

      - name: Playwright Report Summary
        if: always() && steps.check.outputs.should_run == 'true'
        uses: daun/playwright-report-summary@v3
        with:
          report-file: apps/cool-app/playwright-report.json
          comment-title: '🎭 Playwright Test Results'
          report-url: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/${{ steps.pages-path.outputs.path }}/html-report/
          job-summary: true
          icon-style: emojis

      - name: Authenticate to Google Cloud
        if: steps.check.outputs.should_run == 'true'
        uses: google-github-actions/auth@v3
        with:
          credentials_json: '${{ secrets.VRT_GCS_CREDENTIALS_JSON }}'

      - name: Workaround for detached HEAD
        if: steps.check.outputs.should_run == 'true'
        run: |
          git checkout ${{ github.head_ref }}

      - name: Compare with baseline
        if: steps.check.outputs.should_run == 'true'
        id: comparison-keys
        working-directory: apps/cool-app
        run: |
          # ベース側（EXPECTED）
          BASE_HASH=$(git rev-parse --short=7 ${{ github.event.pull_request.base.sha }})
          BASE_VERSION=$(git show ${{ github.event.pull_request.base.sha }}:apps/cool-app/app.json | jq -r '.expo.version')
          BASE_BRANCH="${{ github.event.pull_request.base.ref }}"
          BASE_BRANCH_SAFE=$(echo "$BASE_BRANCH" | sed 's/[^a-zA-Z0-9._-]/_/g')
          EXPECTED_KEY="$BASE_BRANCH_SAFE/$BASE_VERSION/$BASE_HASH/playwright"

          # PR側（ACTUAL）
          PR_HASH=$(git rev-parse --short=7 HEAD)
          PR_VERSION=$(npx expo config --json | jq -r '.version')
          PR_BRANCH="${{ github.head_ref }}"
          PR_BRANCH_SAFE=$(echo "$PR_BRANCH" | sed 's/[^a-zA-Z0-9._-]/_/g')
          ACTUAL_KEY="$PR_BRANCH_SAFE/$PR_VERSION/$PR_HASH/playwright"

          echo "🔍 Playwright VRT Comparison Keys"
          echo "EXPECTED_KEY: $EXPECTED_KEY"
          echo "ACTUAL_KEY: $ACTUAL_KEY"
          echo ""

          # Output for PR comment
          echo "base-hash=$BASE_HASH" >> $GITHUB_OUTPUT
          echo "pr-hash=$PR_HASH" >> $GITHUB_OUTPUT
          echo "expected-key=$EXPECTED_KEY" >> $GITHUB_OUTPUT
          echo "actual-key=$ACTUAL_KEY" >> $GITHUB_OUTPUT

          # reg-suit で比較
          rm -rf .reg
          ACTUAL_DIR=e2e/screenshots \
          EXPECTED_KEY=$EXPECTED_KEY \
          ACTUAL_KEY=$ACTUAL_KEY \
          bunx reg-suit run

          # 比較結果の詳細を出力
          echo ""
          echo "📁 .reg directory structure:"
          ls -lah .reg/
          echo ""
          echo "📄 VRT comparison result (out.json):"
          jq '.' .reg/out.json

          # VRT結果を解析
          CHANGED=$(jq '.diffItems | length' .reg/out.json)
          PASSED=$(jq '.passedItems | length' .reg/out.json)
          NEW=$(jq '.newItems | length' .reg/out.json)
          DELETED=$(jq '.deletedItems | length' .reg/out.json)

          # テーブル行を動的に生成（0以外のみ）
          VRT_TABLE="| Status | Count |
          |--------|-------|"
          [ "$CHANGED" -gt 0 ] && VRT_TABLE="$VRT_TABLE
          | 🔴 Changed | $CHANGED |"
          [ "$PASSED" -gt 0 ] && VRT_TABLE="$VRT_TABLE
          | 🟢 Passed | $PASSED |"
          [ "$NEW" -gt 0 ] && VRT_TABLE="$VRT_TABLE
          | 🆕 New | $NEW |"
          [ "$DELETED" -gt 0 ] && VRT_TABLE="$VRT_TABLE
          | 🗑️ Deleted | $DELETED |"

          # multiline output
          echo "vrt-table<<EOF" >> $GITHUB_OUTPUT
          echo "$VRT_TABLE" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Deploy VRT Report to GitHub Pages
        if: steps.check.outputs.should_run == 'true' && always()
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: apps/cool-app/.reg
          destination_dir: ${{ steps.pages-path.outputs.path }}
          keep_files: true

      - name: Deploy Playwright HTML Report to GitHub Pages
        if: steps.check.outputs.should_run == 'true' && always()
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: apps/cool-app/playwright-report
          destination_dir: ${{ steps.pages-path.outputs.path }}/html-report
          keep_files: true

      - name: Upload VRT report
        if: steps.check.outputs.should_run == 'true' && always()
        uses: actions/upload-artifact@v5
        with:
          name: playwright-vrt-report
          path: apps/cool-app/.reg/
          retention-days: 30
          include-hidden-files: true

      - name: Upload Playwright HTML report
        if: steps.check.outputs.should_run == 'true' && always()
        uses: actions/upload-artifact@v5
        with:
          name: playwright-html-report
          path: apps/cool-app/playwright-report/
          retention-days: 30

      - name: Comment PR with VRT access methods
        if: steps.check.outputs.should_run == 'true' && always()
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: playwright-vrt-report
          message: |
            ## 🎭 Playwright VRT Report

            ${{ steps.comparison-keys.outputs.vrt-table }}

            🔗 **View Report**: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/${{ steps.pages-path.outputs.path }}/

            <details>
            <summary>📋 詳細情報</summary>

            ### ⏱️ 保存期間
            このレポートは **30日間保存** されます（その後自動削除）。

            ### 💻 ローカルでVRTレポートを表示
            ```bash
            cd apps/cool-app
            rm -rf .reg
            mkdir -p .reg
            gsutil -m cp -r "gs://vrt-sample/${{ steps.comparison-keys.outputs.actual-key }}" .reg
            open .reg/playwright/index.html
            ```

            ### 📊 比較情報
            - **Base**: `${{ steps.comparison-keys.outputs.expected-key }}`
            - **PR**: `${{ steps.comparison-keys.outputs.actual-key }}`

            ---
            *Powered by reg-suit + Playwright + GitHub Pages*

            </details>

      - name: Summary
        if: steps.check.outputs.should_run == 'true' && always()
        run: |
          BASE_HASH=$(git rev-parse --short=7 ${{ github.event.pull_request.base.sha }})
          PR_HASH=$(git rev-parse --short=7 HEAD)
          echo "## Playwright VRT Comparison" >> $GITHUB_STEP_SUMMARY
          echo "- Base: $BASE_HASH" >> $GITHUB_STEP_SUMMARY
          echo "- PR: $PR_HASH" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ Playwright VRT比較が完了しました。詳細はアーティファクトのレポートを確認してください。" >> $GITHUB_STEP_SUMMARY
